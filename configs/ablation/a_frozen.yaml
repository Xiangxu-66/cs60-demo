# @package _global_
# A-frozen: Frozen vs Unfrozen encoder ablation
# Verifies: quality cost of freezing is acceptable
encoder:
  frozen: false
training:
  learning_rate: 5.0e-5    # smaller LR when encoder is unfrozen
